# Core Python packages for VNF Performance Testing
prometheus-client>=0.19.0
docker>=7.0.0
pandas>=2.0.0
numpy>=1.24.0
statsmodels>=0.14.0
schedule>=1.2.0
PyYAML>=6.0
requests>=2.31.0
flask>=2.3.0
psutil>=5.9.0

# Deep Learning and ML (CPU-optimized for VM)
# Prefer binary wheels to avoid heavy source builds; allow minor updates
torch>=2.1,<2.4
torchvision>=0.16,<0.19
torchaudio>=2.1,<2.4
scikit-learn>=1.3.0

# Network and SDN (lightweight alternatives)
# mininet>=2.3.0  # Commented out - requires root privileges
# ryu>=4.34       # Commented out - heavy dependency

# Monitoring and Visualization
grafana-api>=1.0.3
matplotlib>=3.7,<3.9
seaborn>=0.12.0

# Development and Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Async and Concurrency
aiohttp>=3.8,<3.10

# Utilities
python-dotenv>=1.0.0
click>=8.1.0
rich>=13.0.0

# Additional VNF-specific dependencies
# Note: asyncio, concurrent-futures, dataclasses are built-in Python modules
typing-extensions>=4.8.0
